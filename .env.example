# Spotify API credentials (https://developer.spotify.com/dashboard)
SPOTIFY_CLIENT_ID=your_spotify_client_id
SPOTIFY_CLIENT_SECRET=your_spotify_client_secret
SPOTIFY_REFRESH_TOKEN=your_spotify_refresh_token

# Lidarr (optional - for torrent-based downloads)
LIDARR_URL=http://lidarr:8686
LIDARR_API_KEY=your_lidarr_api_key

# Music directory
MUSIC_DIR=/music
HOST_MUSIC_DIR=/path/to/your/music/library

# Navidrome (optional - for "In Library" detection and playlist sync)
NAVIDROME_URL=http://navidrome:4533
NAVIDROME_USER=your_navidrome_username
NAVIDROME_PASSWORD=your_navidrome_password

# Last.fm (optional - for Discover tab)
LASTFM_API_KEY=your_lastfm_api_key

# slskd / Soulseek (optional - configure credentials in slskd-data/slskd.yml)
# API key must match the key in slskd.yml
SLSKD_API_KEY=your_slskd_api_key
# Path inside the MusicSeeker container where completed slskd downloads are found (not SLSKD_URL)
# SLSKD_DOWNLOAD_DIR=/downloads

# Admin credentials (first user created on startup)
ADMIN_USER=admin
ADMIN_PASS=change_me_please

# JWT Secret (optional - auto-generated and persisted if not set)
# JWT_SECRET=your_random_secret_here

# GitHub integration (optional - only needed to promote in-app feedback into GitHub issues)
# Fine-grained PAT scoped to this one repo: Issues=RW, Contents=RW, Metadata=R
# GITHUB_TOKEN=github_pat_xxx
# GITHUB_REPO=lucashanak/music-seeker
# GITHUB_SCREENSHOT_BRANCH=bug-screenshots
